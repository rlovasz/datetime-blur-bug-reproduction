import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'datetime-blur-bug-reproduction',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
